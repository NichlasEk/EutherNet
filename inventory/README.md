# Inventory

Generated, reviewed server inventory belongs here.

Run:

```sh
make inventory
```

That writes:

- `inventory/server.md`
- `inventory/server.toml`

Commit those files only after reviewing the generated output for sensitive data
and operational accuracy.
